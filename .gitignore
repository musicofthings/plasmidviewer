# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Build info / test coverage
*.tsbuildinfo
coverage

# Local agent/session state — machine-specific, not project content
.claude/session/
.claude/settings.local.json
# Handover is a working note for whoever is at this machine, not project documentation.
# Durable decisions belong in docs/ and CLAUDE.md, which are tracked.
session_handover.md

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
